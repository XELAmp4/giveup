import styles from './resultat-timeline.module.sass'


export default function ResultatTimeline({source}) {
  return( 
  <section className={styles.resultat}>
    <h1>La réponse est ...</h1>

  </section>
  )
}